<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_sicc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_sicc"
        android:supportsRtl="true"
        android:theme="@style/Theme.SICC"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".activity_details.InformasiAplikasiActivity"
            android:exported="false" />
        <activity
            android:name=".activity_details.DetailInformationActivity"
            android:exported="true" />
        <activity
            android:name=".authentication.UbahPasswordActivity"
            android:exported="true" />
        <activity
            android:name=".authentication.KodeOTPActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".authentication.LupaPasswordActivity"
            android:exported="true" />
        <activity
            android:name=".authentication.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>